
stabilizer_states = ["%s%s" for ]


def find_stabilizer_group(qubit_state):
    """
    Get the stabilizer group corresponding the qubit_state
    
    Parameters
    ----------
    qubit_state: np.matrix (4x4)
        

    Returns
    -------
    stabilizer: list (length =2)
        The stabilizer group
    """